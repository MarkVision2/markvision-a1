-- ТЗ на монтаж (тема/стиль/вставки) раньше нигде не сохранялось для веб-задач
-- Video Agent — воркер строил обложку и описание только по сценарию озвучки,
-- игнорируя ТЗ. Добавляем столбец, чтобы воркер мог опираться именно на ТЗ.
alter table public.heygen_jobs add column if not exists montage_brief text;
